{
  "name": "worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/sandbox": "0.5.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "wrangler": "4.50.0"
  }
}
